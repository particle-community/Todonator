{
  "name": "todonator",
  "version": "1.0.0",
  "main": "./dist/app.js",
  "type": "module",
  "scripts": {
    "build": "npm run clean && tsc && tsc-alias --resolve-full-paths && xcopy /E /I /H /Y public\\* dist\\",
    "clean": "rmdir /S /Q dist && mkdir dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "tsc-alias": "^1.8.10",
    "typescript": "^5.7.2"
  }
}
